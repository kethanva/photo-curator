# Photo Curator — Python dependencies
# Install with:  pip install -r requirements.txt
#
# CLIP (OpenAI) must be installed separately:
#   pip install git+https://github.com/openai/CLIP.git

# Core ML
torch>=2.1.0
torchvision>=0.16.0

# NOTE: numpy<2.0 and Pillow<10.3 are required by facenet-pytorch and mediapipe
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0,<10.3.0

# Face detection and embeddings
facenet-pytorch>=2.5.3

# Image I/O  (upper bound set by facenet-pytorch + mediapipe compatibility)
pillow-heif>=0.13.0        # HEIC/HEIF support (iPhone photos)

# EXIF metadata
piexif>=1.1.3

# Perceptual hashing
ImageHash>=4.3.1

# Clustering and ML utilities
scikit-learn>=1.3.0
numpy>=1.24.0

# Vector store — local HNSW-backed ANN search (no server required)
# Replaces SQLite BLOB + in-memory DBSCAN for large libraries (50k+ photos)
chromadb>=0.4.22

# Progress display
tqdm>=4.66.0

# Config
PyYAML>=6.0

# Smile / eyes-open detection
mediapipe>=0.10.0

# LAION aesthetic predictor (optional — only needed if use_laion_predictor: true)
# huggingface_hub>=0.20.0
